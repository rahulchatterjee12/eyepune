/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "flowbite.s3.amazonaws.com",
      "images.unsplash.com",
      "static.wixstatic.com",
      "i.ibb.co",
    ],
  },
};

export default nextConfig;
